# Claim vs. Reality Log

Documentation audit for the final submission. Every row below was checked against the actual files and/or a real runtime observation before the docs were corrected. Nothing here is inferred from memory.

| # | Documentation claim | Code or runtime reality | Resolution | Evidence to keep |
|---|---------------------|-------------------------|------------|------------------|
| 1 | Root `README.md` structure tree lists `verify_a.py` under `backend/tests/` and tells readers to run `python tests/verify_a.py` (README lines 29 and 99 before the fix). | The script lives at `backend/scripts/verify_a.py`, and running it directly from `backend/` fails with `ModuleNotFoundError: No module named 'app'` because the script imports `app.models` and `scripts/` is not on the module path. | Fixed README: the tree now shows `backend/scripts/verify_a.py`, and the documented command is `PYTHONPATH=. python scripts/verify_a.py`. | `README.md` (tree + Running Tests section), `backend/scripts/verify_a.py` lines 1–2, session run: direct run → `ModuleNotFoundError`; `PYTHONPATH=. python scripts/verify_a.py` → 8/8 PASS. |
| 2 | `CLAUDE.md` says the validation checks run with `python tests/verify_a.py` and lists `tests/verify_a.py` in the architecture section (CLAUDE.md lines 32 and 49 before the fix). | Same reality as row 1: the file is in `backend/scripts/`, not `backend/tests/`, and needs `backend/` on the import path. | Fixed CLAUDE.md: the command is now `PYTHONPATH=. python scripts/verify_a.py` and the architecture line points at `scripts/verify_a.py`. | `CLAUDE.md` (Commands + Architecture sections), `backend/scripts/verify_a.py`, `backend/tests/` (contains only `conftest.py`, `test_health.py`, `test_tasks.py`). |
| 3 | `backend/README.md` section 7 states: "No `.github/workflows/ci.yml` was found in the repository. [VERIFY]" (backend/README.md lines 127–130 before the fix). | The workflow exists at `.github/workflows/ci.yml`: it triggers on `push` and `pull_request`, runs `ubuntu-latest` with Python 3.11, installs `requirements.txt`, and runs `pytest -v` with no `continue-on-error`/`|| true`/`--exit-zero` shortcuts. The GitHub API reports 10 recorded runs, all with conclusion `success` (checked 2026-08-21). | Rewrote section 7 with the real workflow summary and the run evidence link (`https://github.com/JeanElBeainy/task-tracker/actions/runs/32243302797`). Also updated `AGENTS.md`, which claimed "no active CI workflow is confirmed by the files inspected". | `.github/workflows/ci.yml`, `backend/README.md` section 7, `AGENTS.md` (Confirmed commands section), public GitHub API `actions/runs` response (total_count 10, all `success`). |
| 4 (bonus) | `AGENTS.md` listed `python scripts/verify_a.py` as a confirmed command (AGENTS.md line 28 before the fix). | Running that exact command from `backend/` fails with `ModuleNotFoundError: No module named 'app'`; the working invocation is `PYTHONPATH=. python scripts/verify_a.py`. | Fixed AGENTS.md (and the same command in README/CLAUDE.md, rows 1–2) to the invocation that actually runs. | `AGENTS.md` (Confirmed commands), session run output: plain `python scripts/verify_a.py` → traceback; `PYTHONPATH=. python scripts/verify_a.py` → "--- Part A verifications complete ---", exit 0. |

## How these were verified

- `backend/scripts/verify_a.py` was opened: line 2 is `from app.models import TaskCreate, TaskUpdate, TaskStatus, TaskPriority`, which explains the `ModuleNotFoundError` when `scripts/` (not `backend/`) is the import root.
- `PYTHONPATH=. python scripts/verify_a.py` was re-run in this session: all 8 checks PASS.
- `.github/workflows/ci.yml` was read line by line (triggers, Python version, `pytest -v`, no false-green patterns).
- The GitHub Actions API (`https://api.github.com/repos/JeanElBeainy/task-tracker/actions/runs?per_page=10`) was fetched on 2026-08-21: `total_count: 10`, every returned run has `conclusion: success`; the newest run (id 32243302797) is on the `final-project` branch at the current HEAD commit.
- `git diff --check` passes on the corrected files.
