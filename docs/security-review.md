# Module 5 AI Security Findings Review

Rubric applied: Valid means a real issue in this repo, or a course scope limitation that would matter outside the learning context. False Positive means wrong because of the actual code, severity, behavior, or Task Tracker scope. Noise means technically true but too generic, trivial, or unsupported to become an action item.

The four findings (SEC-01 to SEC-04) came from the assignment prompt. Each was graded only after reading the cited files directly. No new findings were added and no severity was upgraded. Final judgment stays with the student.

| Finding ID | Proposed grade | Reason | Evidence needed or evidence used | Student decision to confirm |
|---|---|---|---|---|
| SEC-01 | Valid | Verified directly. main.py imports no auth dependency and no endpoint uses access control; POST, GET, PATCH, and DELETE endpoints at lines 49 to 189 expose full CRUD and activity reads to any caller. The missing authentication is documented as intentional scope in AGENTS.md line 5 and backend/README.md lines 4 to 5 and 168 to 169. The finding acknowledges the intent and rates severity conditionally (High if network accessible), which matches the evidence. Per the rubric this is the canonical course scope limitation that matters outside the learning context. | main.py (no auth imports or Depends anywhere), AGENTS.md, backend/README.md | TBD |
| SEC-02 | Noise | Every code claim is accurate: description (models.py:36) and assignee (models.py:39) are unbounded optional strings, only title is capped at 200 characters (models.py:56), storage is in memory dicts (storage.py:7 to 8), and /tasks and /activity return full collections (main.py:74 and 125). The frontend also applies no maxlength to description or assignee (index.html:949 and 969). The problem is that the finding is generic API hardening boilerplate rather than a defect with an actionable fix in this scope. The stated risk, unbounded memory growth by an unauthenticated caller, is inherent to the in memory design: task count is unbounded regardless of field lengths, so the proposed schema limits do not close the stated risk, and the attacker premise is the out of scope no authentication decision covered by SEC-01. Within course scope there is no attacker and no deployment, so it does not become an action item. Medium severity overstates it (Low at most). Defensible as Valid at Low only if the student chooses to count production hardening as in scope. | models.py:30 to 58, storage.py:7 to 8 and 36, main.py:74 and 125, frontend/index.html:944 to 969 | TBD |
| SEC-03 | Noise | All facts verified: Dockerfile lines 4 and 19 use python:3.11-slim, ci.yml lines 15 and 17 use actions/checkout@v4 and actions/setup-python@v5, and requirements.txt pins direct dependencies with == but no hashes. However this is standard supply chain advice that applies to every project, and no actual harm is evidenced: nothing is deployed from this image or this workflow, so upstream drift has no concrete consequence within scope. Reproducibility hardening does not become an action item for a learning project. Not False Positive because nothing is misstated. Noise because it is generic and unsupported by any demonstrated impact. | Dockerfile:4 and 19, ci.yml:15 to 17, backend/requirements.txt | TBD |
| SEC-04 | Noise | Verified: the workflow has a single job that installs dependencies and runs pytest (ci.yml lines 15 to 28), and docs/decisions/ci-workflow-design.md line 27 records the minimal pipeline as an intentional choice, with line 51 noting the Docker build is not automated. The finding itself concedes it creates no vulnerability by itself. Absence of a scan is a CI coverage suggestion, not a security defect, and the design note documents the scope decision, so it does not become an action item in this course context. Technically true, hence not False Positive. Generic and non actionable within scope, hence Noise. | ci.yml:1 to 28, docs/decisions/ci-workflow-design.md:27 and 51 | TBD |

Evidence inspected: backend/app/main.py, backend/app/models.py, backend/app/storage.py, backend/Dockerfile, .github/workflows/ci.yml, backend/requirements.txt, AGENTS.md, backend/README.md, docs/decisions/ci-workflow-design.md, frontend/index.html.
